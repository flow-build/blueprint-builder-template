module.exports = {
  id: "{{id}}",
  name: "{{name}}",
  next: "{{next}}",
  type: "UserTask",
  lane_id: "{{lane}}",
  parameters: {
    input: {},
    action: "{{constantCase action}}",
    activity_manager: "{{activityManager}}",
    timeout: {{timeout}},
    activity_schema: {
      type: "object"
    }
  },
  example: {
    file: "{{example}}",
    schema: "{{exampleSchema}}",
  },
};
