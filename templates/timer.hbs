module.exports = {
  id: "{{id}}",
  name: "{{name}}",
  next: "{{next}}",
  type: "SystemTask",
  category: "timer",
  lane_id: "{{lane}}",
  parameters: {
    input: {},
    timeout: {{timeout}}
  }
}